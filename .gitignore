.DS_Store
._*

# Sub-site build output; the assembled copies live in www/
sites/*/out/

# Scratch at the repo root. `1` (an 8.7 MB compiled binary) and `1.raku` were
# both committed by accident — the binary in 43a41d63, the script by a
# `git add -A` — so the root is now explicit about what belongs there.
/1
/1.raku
/*.raku

# Local tool config, not repo content — and it carries absolute home paths.
.claude/
