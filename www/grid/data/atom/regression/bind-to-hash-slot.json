{"atom":"regression/bind-to-hash-slot","family":"regression","slug":"bind-to-hash-slot","header":{"atom":"regression/bind-to-hash-slot","kind":"whole-program — a minimal repro, not yet reduced to an atom","source":"curated"},"axes":null,"cols":null,"engines":["rakudo-2026.07","rakupp-3.14.0"],"kinds":["ref","pp"],"defFrom":"rakupp:t/regression/bind-to-hash-slot.raku","counts":{"a":1,"f":0,"d":0,"c":0,"r":0,"n":0},"files":["atoms/regression/bind-to-hash-slot.grid"],"records":[["0001","","# Regression: `$x := %h{key}` binds the hash SLOT, so a later `$x = v` writes\n# into the hash — including a walk that rebinds one level at a time and\n# autovivifies as it goes. Config's `.set` is written exactly this way.\n# Runs clean under Rakudo too.\n\nmy $fails = 0;\nsub ck($got, $want, $desc) {\n    if $got eqv $want { say \"ok - $desc\" }\n    else { $fails++; say \"FAIL: $desc — {$got.raku} vs {$want.raku}\" }\n}\n\nmy %h = a => 1;\nmy $s := %h<a>;\n$s = 2;\nck %h<a>, 2, 'assigning through the binding writes into the hash';\n\nmy %n = a => { b => 'old' };\nmy $index := %n;\nfor <a b> { $index := $index{$_} }\n$index = 'new';\nck %n<a><b>, 'new', 'a rebinding walk reaches the nested slot';\n\nmy %fresh;\nmy $i2 := %fresh;\nfor <x y z> { $i2 := $i2{$_} }\n$i2 = 'v';\nck %fresh<x><y><z>, 'v', 'and autovivifies the whole path';\n\n# reading through the binding sees later writes to the hash\nmy %r = k => 1;\nmy $b := %r<k>;\n%r<k> = 9;\nck $b, 9, 'the binding reads the current slot, not a copy';\n\n# a binding taken from a hash that is itself shared storage\nmy %outer = inner => {};\nmy %alias := %outer;\nmy $slot := %alias<inner>;\n$slot = 'set';\nck %outer<inner>, 'set', 'through an aliased hash too';\n\nsay $fails ?? \"\\n$fails FAILED\" !! \"\\nPASS\";\nexit $fails ?? 1 !! 0;","output ok - assigning through the binding writes into the hash\nok - a rebinding walk reaches the nested slot\nok - and autovivifies the whole path\nok - the binding reads the current slot, not a copy\nok - through an aliased hash too\n\nPASS","a",["ok - assigning through the binding writes into the hash …(7 lines)","ok - assigning through the binding writes into the hash …(7 lines)"],0,[["budget","20s"]]]]}